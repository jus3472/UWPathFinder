import SwiftUI

/// Main entry point for the UW Path Finder application.
/// Configures and launches the app with MainView as the root view.
@main
struct UWPathFinderApp: App {
    var body: some Scene {
        WindowGroup {
            MainView()
        }
    }
}
